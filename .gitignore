# Database credentials — created from api/config.sample.php, never committed.
api/config.php

# Runtime data — created on first run, never committed.
# Covers any legacy SQLite database plus its -journal / -wal / -shm sidecars.
data/forge.sqlite*
data/jobs/*
!data/jobs/.gitkeep

# Editor / OS noise
.DS_Store
*.swp
node_modules/
