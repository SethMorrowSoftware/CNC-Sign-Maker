# Runtime data — created on first run, never committed.
# Covers the database plus any -journal / -wal / -shm sidecar files.
data/forge.sqlite*
data/jobs/*
!data/jobs/.gitkeep

# Editor / OS noise
.DS_Store
*.swp
node_modules/
